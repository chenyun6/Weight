<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.infrastructure.weight.repository.mapper.WeightRecordMapper">

    <select id="findByUserIdAndDate" resultType="com.cy.infrastructure.weight.repository.model.WeightRecordDO">
        SELECT *
        FROM weight_record
        WHERE user_id = #{userId}
          AND record_date = #{recordDate}
        LIMIT 1
    </select>

    <select id="findLastRecordDate" resultType="java.time.LocalDate">
        SELECT record_date
        FROM weight_record
        WHERE user_id = #{userId}
        ORDER BY record_date DESC
        LIMIT 1
    </select>

</mapper>
